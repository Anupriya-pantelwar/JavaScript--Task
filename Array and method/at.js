// at:- to fetch the  elemnet from last and first
// if we give -ve value then it gives elements from last position
// if +ve value then it give element from first position

const data = ["a", "b", "c", "d", "e"];
console.log(data.at(-1));

console.log(data.at(2));
